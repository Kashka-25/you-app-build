import TreeOfStars from "./tree-stars/TreeOfStars";

// The Story of You cosmos now lives inside the tree scene itself — a
// floating orb that zooms open into it — rather than a separate section
// you had to scroll to find.
export default function TreeAndStarsView() {
  return <TreeOfStars />;
}
