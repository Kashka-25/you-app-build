import { ParkedScreen, Placeholder } from "../Primitives";

export default function Saved() {
  return (
    <ParkedScreen title="Saved" note="future — design direction only">
      <Placeholder label="saved item">repeat</Placeholder>
    </ParkedScreen>
  );
}
