import { ParkedScreen, Placeholder } from "../Primitives";

export default function Empatherapy() {
  return (
    <ParkedScreen title="Empatherapy" note="parked — separate mission, bridge only">
      <Placeholder label="browse practitioners">
        Bridge card only — full Empatherapy platform lives outside YOU Core.
      </Placeholder>
    </ParkedScreen>
  );
}
