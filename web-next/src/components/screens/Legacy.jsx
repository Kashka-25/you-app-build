import { ParkedScreen, Placeholder } from "../Primitives";

export default function Legacy() {
  return (
    <ParkedScreen title="Legacy mode" note="future — design direction only">
      <Placeholder label="your legacy">
        Life chapters, photos, journal entries, voice notes, lessons — exported as memoir.
      </Placeholder>
    </ParkedScreen>
  );
}
