# Constants

Static data the app reads but never mutates.

- values.const.js — the value library (12 values, each with challenges). Edit to add values.
- levels.const.js — avatar SVG artwork per evolution stage.

(Numeric constants like XP values and level thresholds live in ../config.js.)
