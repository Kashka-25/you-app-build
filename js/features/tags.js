// tags.js - Tags tab grouping
// Part of the YOU app. Loaded via index.html in dependency order.

function renderTags(){var el=document.getElementById("tagsList");if(!el)return;var allTags={};S.items.forEach(function(item){(item.tags||[]).forEach(function(tag){if(!allTags[tag])allTags[tag]={active:[],achieved:[]};if(item.done)allTags[tag].achieved.push(item);else allTags[tag].active.push(item);});});var tagNames=Object.keys(allTags).sort();if(tagNames.length===0){el.innerHTML="<div class=\"tags-empty\">Add tags to your items to see them organised here...</div>";return;}el.innerHTML=tagNames.map(function(tag){var data=allTags[tag];var total=data.active.length+data.achieved.length;var activeHtml=data.active.map(function(i){return cardHtml(i,true);}).join("");var achievedHtml=data.achieved.map(function(i){return cardHtml(i,true);}).join("");return "<div class=\"tags-section\"><div class=\"tags-section-header\"><span class=\"tags-section-name\">#"+tag+"</span><span class=\"tags-section-count\">"+total+" items</span></div>"+(data.active.length>0?"<div class=\"tags-section-divider\">In progress</div><div class=\"items-grid\">"+activeHtml+"</div>":"")+(data.achieved.length>0?"<div class=\"tags-section-divider\">Achieved</div><div class=\"items-grid\">"+achievedHtml+"</div>":"")+"</div>";}).join("");}
